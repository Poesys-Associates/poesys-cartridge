/**
 * Copyright 2009 Poesys Associates. All rights reserved.
 */
// Generated by AndroMDA Poesys/DB cartridge: DO NOT EDIT DIRECTLY.
// Template: AbstractUpdate.vsl

package com.poesys.db.memcached_test.sql;


import java.sql.PreparedStatement;
import java.sql.SQLException;

import com.poesys.db.dao.update.IUpdateSql;
import com.poesys.db.pk.IPrimaryKey;


/**
 * SQL statement specification for updating a MapElement with read/write properties
 * 
 * @author Robert J. Muller
 */
public class AbstractUpdateMapElement implements IUpdateSql<com.poesys.db.memcached_test.IMapElement> {
  /** SQL UPDATE statement for MapElement */
  private static final String SQL =
    "UPDATE MapElement SET elementType = ?, mapId = ?, polyId = ? WHERE ";

  @Override
  public String getSql(IPrimaryKey key) {
    StringBuilder builder = new StringBuilder(SQL);
    builder.append(key.getSqlWhereExpression(""));
    return builder.toString();
  }

  @Override
  public int setParams(PreparedStatement stmt, int index, com.poesys.db.memcached_test.IMapElement object)
      throws SQLException {
    stmt.setString(index, object.getElementType());
    index++;
    // sets key params for required map object
    index = object.getMap().getPrimaryKey().setParams(stmt, index);
    // sets key params for required poly object
    index = object.getPoly().getPrimaryKey().setParams(stmt, index);
    // sets primary key in where clause
    index = object.getPrimaryKey().setParams(stmt, index);
    return index;
  }
}
